<div class="col-md-6">
  <ul>
  {{#if model.skills.length}}
    {{#each model.skills as |skill|}}
    <p id="cat-link">{{#link-to 'sections.section.skill' skill.id}}{{skill.name}}{{/link-to}}</p>
    {{/each}}
  {{else}}
      <i>No skill modifiers for this category</i>
  {{/if}}
  </ul>
</div>
<div class="row">


  <br>

  <br>
  {{outlet}}
  <div class="col-md-12">
    <br>
    {{section-chart model=model}}
  </div>

</div>
